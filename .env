# TaskFlow / Supabase config
# NOTE: VITE_ prefixed vars are bundled into the client build.
# The anon key is safe to expose ONLY when RLS policies are restrictive.
# Until auth is wired up, RLS allows anon full access — anyone with this
# key can read/write the data. Tighten policies before going wide.
VITE_SUPABASE_URL=https://pybajqcgjlzrxdsfepnp.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_barT5DaWFvjncgb1Hrlyvg_TviXq7zY
